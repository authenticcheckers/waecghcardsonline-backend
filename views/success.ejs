<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Voucher — waecghcardsonline</title>
  <style>
    body{ font-family: system-ui, -apple-system, Arial; background:#f6f8fb; color:#0b1a2b; padding:24px; }
    .card{ max-width:720px; margin:0 auto; background:#fff; border-radius:10px; padding:20px; box-shadow:0 8px 30px rgba(11,26,43,0.07); }
    h1{ margin:0 0 8px 0; font-size:20px; }
    pre{ background:#f4f6fb; padding:12px; border-radius:6px; overflow:auto; }
    .meta{ margin-top:12px; color:#556; font-size:14px; }
    .actions{ margin-top:18px; display:flex; gap:8px; }
    button{ padding:10px 14px; border-radius:8px; border:0; cursor:pointer; font-weight:600; }
    .btn-primary{ background:#1f7aef; color:#fff; }
    .btn-ghost{ background:transparent; border:1px solid #dfe8fb; }
    .signature{ margin-top:18px; }
    img.signature-img{ max-width:180px; display:block; }
  </style>
</head>
<body>
  <div class="card" role="main">
    <h1>Your WASSCE Voucher</h1>
    <p>Below is your voucher. Save or print it now — you can also download it as PDF via Print &rarr; Save as PDF.</p>
    <pre id="voucherText"><%= voucher %></pre>

    <div class="meta">
      <div>Serial: <strong><%= serial || '' %></strong></div>
      <div>PIN: <strong><%= pin || '' %></strong></div>
    </div>

    <div class="signature">
      <% if (signaturePath) { %>
        <div>Authorized by:</div>
        <img class="signature-img" src="<%= signaturePath %>" alt="signature"/>
      <% } %>
    </div>

    <div class="actions">
      <button class="btn-primary" id="printBtn">Print / Save PDF</button>
      <button class="btn-ghost" id="copyBtn">Copy to clipboard</button>
    </div>
  </div>

  <script>
    document.getElementById('printBtn').addEventListener('click', ()=> window.print());
    document.getElementById('copyBtn').addEventListener('click', async ()=>{
      try {
        await navigator.clipboard.writeText(document.getElementById('voucherText').innerText);
        alert('Voucher copied to clipboard');
      } catch(e) { alert('Copy failed'); }
    });
  </script>
</body>
</html>
